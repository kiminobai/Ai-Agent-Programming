# @langchain/community exposes optional integrations such as Stagehand.
# This project only uses its document loaders, so npm should not fail on
# peer conflicts from optional integrations that are not installed or used.
legacy-peer-deps=true
